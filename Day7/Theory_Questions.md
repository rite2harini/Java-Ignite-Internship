1)Explain the difference between a Class and an Object in Java with a real-life example.
Also explain why we cannot directly execute a class without creating an object.

eg for making a car we need a blue print that contains the parts its works -class
the honda city is a car which is the object to make the car we need class
hondacity car cannot be created without the blueprint.

class contains information or data of a particular type like here car
but exactly which object we need to create is not specifed 



2)What are access modifiers in Java?
Explain the difference between public and private with examples.
Why is it not safe to make all variables public?


ans-Acess modifers are the keywords which  decides the acsses of the variables 
there are 4 -public private protected default
public means the variable can be asses inside and outside the class
if it is private then the variable can be acessed only inside the class not outside, we use getters and seeters to acess them outside
it is because we donot want the user to know some details or to change some details like for eg bank passsword



3)Explain how encapsulation is achieved using private variables and public methods.
Give a real-life example (bank, mobile, ATM, etc.).
for bank-bankpas must be private account number must be private accountholder public
mobile-price must be private
atm-pin must be private



4)What is a constructor in Java?
Explain how the this keyword is used inside a constructor.
Why do we use constructors to initialize object values?

constructor is a code that is automatically called when an object of a class is created it doesnot have any return type
this keyword helps us to point the object eg-this.name

because it helps the object to be in a valid state with set number of data 
