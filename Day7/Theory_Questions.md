1)Explain the difference between a Class and an Object in Java with a real-life example.
Also explain why we cannot directly execute a class without creating an object.
class is a blueprint used to create objects and object is an instance of class. A class cant be directly executed without creating object.
example-class-flowers and objects can be roses,sunflowers,lotus



2)What are access modifiers in Java?
Explain the difference between public and private with examples.
Why is it not safe to make all variables public?
they are the keywords to control the visibility of variables,classes,methods


3)Explain how encapsulation is achieved using private variables and public methods.
Give a real-life example (bank, mobile, ATM, etc.).
it is the process of hiding data and giving controlled access through methods by declaring variables a private and accessing them by public methods.ex-atm allows user to check balance and withdraw money without directtly accessing bank records


4)What is a constructor in Java?
Explain how the this keyword is used inside a constructor.
Why do we use constructors to initialize object values?
it is a method that is automatically called when an object is created.
this keyword is used to distinguish instance variable from local variables.
consstructors automatically initialie object values when object is created,which makes the process easier
